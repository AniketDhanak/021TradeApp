import 'dart:ui';

class AppColors {
  // Primary Brand Colors
  static const primaryGold = Color(0xFFD4AF37);   // Main gold
  static const lightGold = Color(0xFFFFD700);     // Highlight gold
  static const darkGold = Color(0xFFB8962E);      // Pressed / darker

  // Backgrounds
  static const background = Color(0xFFF8F9FA);    // Light background
  static const card = Color(0xFFFFFFFF);          // Pure white card
  static const textFieldBackground = Color(0xFFFFF9F9);          // Pure white card

  // Text Colors
  static const textPrimary = Color(0xFF1A1A1A);   // Dark text
  static const textSecondary = Color(0xFF6B7280); // Grey text

  // Trading Colors
  static const profitGreen = Color(0xFF16A34A);
  static const lossRed = Color(0xFFDC2626);

  // Borders / Divider
  static const border = Color(0xFFE5E7EB);
}